# Supabase Edge Functions Configuration

[functions.stripe-webhook]
# Disable JWT verification for Stripe webhook
# Stripe authenticates via stripe-signature header, not Supabase JWT
verify_jwt = false

# AI Edge Functions (require authentication by default)
# These functions call OpenAI/Gemini APIs with secrets stored server-side
[functions.ai-invoke-llm]
# verify_jwt = true (default - requires user authentication)

[functions.ai-generate-image]
# verify_jwt = true (default - requires user authentication)
